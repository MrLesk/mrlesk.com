---
layout: default
mood: 4
---

<AttemptRail :current="2" />

# It worked. On its own repo.

<div class="attempt attempt-split">
  <div class="lines">
    <p>Decent code-to-architecture generation.</p>
    <p class="soft">It failed on anything else. The AI proposed to hardcode every possible rule.</p>
  </div>
  <div class="meme">
<pre>if (n == 0) printf(<b>"even\n"</b>);
else if (n == 1) printf(<b>"odd\n"</b>);
else if (n == 2) printf(<b>"even\n"</b>);
else if (n == 3) printf(<b>"odd\n"</b>);
else if (n == 4) printf(<b>"even\n"</b>);
else if (n == 5) printf(<b>"odd\n"</b>);</pre>
    <span>Remember this?</span>
  </div>
</div>

<!--
⏱ 25:00

Groma 2. Decent code to architecture generation, for its own repo. It failed on anything else.
The AI proposed to hardcode every possible rule. Remember this one? That is what it wanted to do.

This is why today's scanners are deterministic and general.
-->
