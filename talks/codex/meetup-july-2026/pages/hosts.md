---
title: VAIE & Volee
hideInToc: true
---

<div class="sponsor-grid hosts-grid">

<div class="sponsor-text">

  <p class="vaie-tagline hosts-tagline">
    We talk about the art and craftsmanship of creating reliable AI-powered applications <span class="vaie-tagline-muted">that users love</span>.
  </p>

  <div class="volee-card">
    <div class="volee-eyebrow">Venue partner</div>
    <div class="volee-name">Volee</div>
    <p>Thank you for hosting us in Prater tonight.</p>
  </div>

</div>

<div class="hosts-panel">
  <div class="hosts-panel-eyebrow">Organized by</div>
  <span class="vaie-mark hosts-mark">
    <span class="vaie-text">
      <span class="vaie-line-1">
        <span class="vaie-vienna">Vienna</span>
        <span class="vaie-ai">AI</span>
      </span>
      <span class="vaie-eng">Engineering</span>
    </span>
    <img class="vaie-logo" src="/vaie-logo.png" alt="" />
  </span>

  <img class="hosts-qr" src="/vaie-meetup-qr.png" alt="QR code to the Vienna AI Engineering meetup group" />

  <div class="hosts-qr-caption">
    <strong>Join the group &amp; catch our next events</strong>
    <a href="https://www.meetup.com/de-DE/vienna-ai-engineering/" target="_blank">meetup.com/vienna-ai-engineering</a>
  </div>
</div>

</div>

<style>
.hosts-grid {
  align-items: center;
  gap: calc(56 * var(--pt));
  margin-top: calc(14 * var(--pt));
}

.hosts-tagline {
  color: #dfe5ff;
  font-size: calc(26 * var(--pt));
  line-height: 1.3;
  margin: calc(6 * var(--pt)) 0 0 0;
}

.volee-card {
  margin-top: calc(34 * var(--pt));
  background: var(--codex-glass);
  border: 1px solid var(--codex-border);
  border-radius: calc(12 * var(--pt));
  padding: calc(16 * var(--pt)) calc(20 * var(--pt));
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
  max-width: calc(420 * var(--pt));
}

.volee-eyebrow {
  font-size: calc(11 * var(--pt));
  text-transform: uppercase;
  letter-spacing: 0.16em;
  color: var(--codex-accent);
  font-weight: 500;
  margin-bottom: calc(6 * var(--pt));
}

.volee-name {
  font-size: calc(30 * var(--pt));
  font-weight: 700;
  letter-spacing: 0.04em;
  line-height: 1;
  margin-bottom: calc(6 * var(--pt));
}

.volee-card p {
  margin: 0;
  font-size: calc(14 * var(--pt));
  color: var(--codex-fg-muted);
}

/* One tall white brand panel: VAIE lockup in original navy-on-white,
   QR beneath it. Mirrors the June save-the-date panel. */
.hosts-panel {
  min-height: calc(400 * var(--pt));
  background: #ffffff;
  border-radius: calc(16 * var(--pt));
  padding: calc(28 * var(--pt)) calc(24 * var(--pt));
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: calc(20 * var(--pt));
  box-shadow: 0 calc(14 * var(--pt)) calc(40 * var(--pt)) rgba(0, 0, 0, 0.55);
}

.hosts-panel-eyebrow {
  font-size: calc(11 * var(--pt));
  text-transform: uppercase;
  letter-spacing: 0.18em;
  color: #6c6d70;
  font-weight: 600;
}

.hosts-mark {
  gap: calc(12 * var(--pt));
}

.hosts-mark .vaie-line-1,
.hosts-mark .vaie-eng {
  font-size: calc(24 * var(--pt));
}

.hosts-mark .vaie-text {
  gap: calc(4 * var(--pt));
}

.hosts-mark .vaie-logo {
  height: calc(60 * var(--pt));
}

.hosts-mark .vaie-vienna { color: #6c6d70; }
.hosts-mark .vaie-ai,
.hosts-mark .vaie-eng { color: #00275c; }

.hosts-qr {
  width: calc(170 * var(--pt));
  height: calc(170 * var(--pt));
  display: block;
  image-rendering: pixelated;
}

.hosts-qr-caption {
  text-align: center;
  display: flex;
  flex-direction: column;
  gap: calc(3 * var(--pt));
}

.hosts-qr-caption strong {
  color: #10162c;
  font-size: calc(14 * var(--pt));
  font-weight: 650;
}

.hosts-qr-caption a {
  font-family: 'JetBrains Mono', ui-monospace, monospace;
  font-size: calc(12 * var(--pt));
  color: #3442e0;
  border-bottom: none;
}
</style>

<!--
VAIE: Vienna AI Engineering. Bogdan and I run this community; the Codex meetups are VAIE events.
Scan the QR to join the meetup group and hear about the next ones first.
And a big thank you to Volee for opening their doors in Prater for us tonight.
-->
